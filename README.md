(
echo # VQA Text Encoding Models + L2 Regularization
echo.
echo This repo contains the model variants used in the paper "VQA Model: Based on Different Text Encoding Models and L2 Regularization".
echo.
echo ## Contents
echo - models/: RNN, GRU, LSTM, Additive-Attention variants, TransformerEncoder
echo - paper/: manuscript (DOCX)
echo.
echo ## Note
echo Dataset and training pipeline are not included in this repo.
) > README.md
